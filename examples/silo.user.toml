[general]
user = "markus"

[shared_volume]
paths = [
  "$HOME/.local/share/fish/fish_history",
  "$HOME/.claude.json",
  "$HOME/.claude/",
  "$HOME/.cache/opencode/",
  "$HOME/.local/share/opencode/",
  "$HOME/.local/state/opencode/"
]

[podman]
create_args = []
