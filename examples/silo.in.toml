[features]
podman = false

[shared_volume]
name  = "silo-shared"
paths = [
  "$HOME/.local/share/fish/fish_history",
  "$HOME/.claude.json",
  "$HOME/.claude/",
  "$HOME/.cache/opencode/",
  "$HOME/.local/share/opencode/",
  "$HOME/.local/state/opencode/"
]

[podman]
create_args = []
